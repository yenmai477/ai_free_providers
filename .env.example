# Shared gateway master key (LiteLLM). Generate a random string; never commit real keys.
GATEWAY_API_KEY=change-me-to-a-long-random-secret

# Set after Colab prints the Cloudflare URL (do not hard-code in repo).
# ANTHROPIC_BASE_URL=https://xxxxx.trycloudflare.com
# ANTHROPIC_API_KEY=${GATEWAY_API_KEY}
